# مسجّل البيانات التاريخي (recorder)

> جزء من مشروع **aoi** — انظر [الوثيقة الرئيسية](../README.md) للمعمارية
> الكاملة و[خطة ما بعد الجمع](../docs/PLAN.md). هذا المجلّد يضمّ **المسجّل**
> (`FomoRecorder`) و**الموسِّم** (`FomoLabeler`) و**بانيَ صفوف التدريب**
> (`FomoBuildRows`).

يجمع بيانات fomo.family عبر الزمن لتدريب نموذج يتنبّأ بأي عملة ستصعد. النظام
يقرأ fomo حيّاً لكنه لا **يحفظ** الحالة عبر الزمن؛ هذا المسجّل يسدّ تلك الفجوة:
يلتقط **الحالة عند لحظة الإشارة (t=0)** ثم **يتابع السوق كل دقيقة لمدّة 48 ساعة**
حتى يمكن اشتقاق النتيجة (هل صعدت؟) لاحقاً في مرحلة الـ labeling.

**المسجّل والموسِّم يعملان الآن** (`FomoRecorder` + `FomoLabeler`). النموذج
الاستكشافي v1 والتداول الورقي الاستكشافي يبدآن عند 100 ضابطة v3 ناضجة، أما
النموذج المعتمد وتجربة الورق الجديدة فينتظران 500 ضابطة و14 يوماً واجتياز
بوابة المرحلة 1 — الخطة التفصيلية في
[docs/PLAN.md](../docs/PLAN.md). لا يمكن تسجيل الماضي (إلّا جزئياً، انظر أدناه)
— كل يوم تأخير = بيانات مفقودة.

## لماذا يقرأ الخام مباشرة

`FomoClient._map_trending_token` يُسقط أثمن الحقول (change5m…24، volume…،
txnCount، buy/sellCount، uniqueBuys، holders، top10HoldersPercent، mintable،
freezable، creatorAddress، socialLinks، circulating/totalSupply). لذلك المسجّل
يستدعي `_get`/`_post` الخام **قبل أي تعيين** ويستخرج الحقول بنفسه في `extract.py`.

## المبادئ الحاكمة

- **الخام دائماً**: كل صفّ يحفظ `raw_json` بجانب الحقول المستخرجة — أي ميزة نكتشفها
  لاحقاً نعيد اشتقاقها من الأرشيف دون خسارة الماضي. الخام **مضغوط بـ zlib**
  (انظر «تخزين الخام» أدناه) — لا يُقرأ بـ `json.loads` مباشرة.
- **منع تسرّب المستقبل**: المسجّل لا يحسب أي label — يسجّل خاماً بختم زمني فقط.
- **مقاومة انحياز البقاء**: كل عملة دخلت بإشارة تُراقَب 48 ساعة بغضّ النظر عن
  النتيجة — نسجّل الخاسرة كما الرابحة.
- **لا فبركة (FR-007)**: الحقل الغائب = NULL، لا صفر مفترض. False محفوظ صراحةً
  (freezable=0 يعني "آمن"، مختلف عن NULL "مجهول").
- **قراءة فقط (FR-012)**: كل النداءات GET/POST قراءة (feed، trending، verified،
  leaderboard). لا نداء يكتب حالة حساب أو تداول.
- **النجاة من الأعطال**: فشل أي نداء (502…) يُسجَّل في `meta` ويُتخطّى؛ الحلقة لا
  تموت. درع أخير حول الدورة كلها يلتقط أي انهيار غير متوقّع.

## الإشارات (المُشغّلات)

`multi_user_buy` و`large_buy` هما ما **يُدخل** عملة إلى المراقبة. `multi_user_sell`
و`large_sell` يُسجَّلان كسياق لا يُشغّلان مراقبة (الثاني مؤكَّد حيّاً 2026-07-28 —
تصريف حوت مفرد بنفس شكل `large_buy` باتجاه معاكس). الرواج/التحذيرات ميزات لا
مُشغّلات. الموسِّم يوسم **كل** الإشارات متى توفّرت الشموع، فيتراكم مجاناً سجلّ
«بيع حوت/متصدّر → ماذا حدث للسعر» — مدخل نموذج الخروج لاحقاً (انظر
[docs/PLAN.md](../docs/PLAN.md) §2.3-ك).

**إشارة "أكثر من متصدّر اشترى"**: لكل حدث نطابق `topTraders[].id` بصدارة
المتصدّرين (تُحمَّل كل ساعة) ونسجّل `top_trader_match_count` (كم متصدّراً اشترى)
و`buyers_best_rank` (أفضل رتبة) — إشارة أقوى من الشراء المتعدّد العام.

**أربع صدارات لا واحدة** (منذ 2026-08-10): المصدر يسقّف `/v2/leaderboard` عند
**50** متصدّراً ويُهمل كل صيغ الترقيم بصمت — تسع صيغ مفحوصة حيّاً
(`limit`/`offset`/`skip`/`page`/`pageNumber`/`start`/`from`/`pageSize`/`take`)،
وقوائم المعرّفات متطابقة بايتاً، فـ`LEADERBOARD_SIZE = 200` كان بلا أثر منذ
كُتب. لكنّ `/24h` و`/7d` و`/30d` تعيد كلٌّ **100** (مقيس على الخام المؤرشف):
الاتّحاد **214** متداولاً مميّزاً، 164 منهم لا تعرفهم الصدارة الأساسيّة. مقيس
على 7,200 حدث شراء حقيقيّ من ثلاثة أيام: الخمسون تطابق 265 حدثاً (3.68%)
والاتّحاد 1,099 (15.26%) — **×4.15**. الرتب **منفصلة بالمدّة**
(`buyers_best_rank_24h/_7d/_30d`) لا مدموجة في رقم واحد: رتبة 7 في `24h` ليست
رتبة 7 في `totalPnL`، ودمجهما يخلط قياسين. ويُضاف `top_trader_periods_matched`
(في كم صدارة من أربع ظهر مشترٍ) — متصدّر الأربع كلّها سِجلّ، ومتصدّر `24h`
وحدها قد يكون ضربة حظّ. و`best_rank_any_period` مداه 1-100 لا 1-50، فيستعيد
`rank_le_50` معناه على العائلة الجديدة.

⚠️ **الماضي لا يُعبّأ**: الأرشيف حفظ `totalPnL` وحدها، فلا تاريخ لرتب المدد.
عائلة v7 تبقى NULL في كل صفّ سابق ومقيسة في الجديد. لا التفاف على ذلك؛
الالتفاف تعبئةٌ رجعيّة، وهي مرفوضة (`is_live=1` حصراً).

## الجداول (`schema.sql`)

- `watchlist` — من نراقب، مصدر الدخول، ومتى تنتهي (48 ساعة). العملة النشطة لا
  تُمدَّد بإشارة جديدة (أوّل ظهور هو المرجع)، لكنّ العملة **المنتهية تعود** بنافذة
  جديدة إن أشارت إليها إشارة لاحقة — بلا ذلك كانت القائمة تنزف حتى الصفر.
- `signal_events` — لحظة القرار t=0 مع مطابقة المتصدّرين والخام.
- `market_ticks` — لقطة سوق كاملة، لكن **فقط للعملات التي تظهر في trending/verified**.
- `token_bars` — شموع OHLCV لكل عملة مراقَبة بدقّة 5 دقائق (مصدر الحقيقة السعرية
  للتوسيم)، **وللمرجعيات الكلّية SOL/WETH/WBTC بدقّة ساعية** (تُسحب كل ساعة منذ
  2026-07-28 — مرجع النظام السوقي؛ لا watch لها فلا يلمسها الموسِّم).
  أعمدة `h_suspect`/`l_suspect`/`c_suspect` تعلّم القيم المستحيلة من المنبع
  (انظر «سلامة الشموع» أدناه) — القيمة الخام تبقى كما وردت.
- `bars_fetch_state` — حالة آخر سحب شموع لكل عملة (تقود الجدولة الدوّارة).
- `token_static` — ثوابت العملة (mint/freeze/creator/socials) مرّة عند الدخول.
- `snapshots` — أرشيف خام دوري لكل مصدر (feed/trending/verified/**leaderboard** —
  الصدارة الخام كل ساعة منذ 2026-07-28؛ قبلها كانت تُقرأ وتُرمى فضاع مسار
  المتصدّرين نهائياً). ومنذ 2026-08-10 لكل مدّة مصدرها المستقلّ
  (`leaderboard` · `leaderboard_24h` · `leaderboard_7d` · `leaderboard_30d`) —
  دمجها في مصدر واحد يخلط أربع قوائم في أرشيف لا يُفكّ. **المؤرشَف طازج فقط**:
  مدّة تفشل في ساعةٍ لا يُعاد ختم مغلّفها القديم بتوقيت تلك الساعة، وإلّا صارت
  في الأرشيف صدارةٌ لم تُجلب في وقتها — وهو تلويث لا يُكشف لاحقاً.
- `token_social` — سلسلة زمنية للزخم الاجتماعي لكل عملة مراقَبة.
- `token_thesis` — صفّ لكل أطروحة بختم كتابتها (يتيح العدّ **التاريخي**).
- `activity_events` — أحداث tradingActivity التاريخية (مشيّاط رجعيّ حتى 2025-10-22):
  أحداث `multi_user_*` **بنفس معرّفات /feed** + أحداث `swap_buy/swap_sell` فردية
  بـ`usd_amount` لا يعرضها الـ feed. يملؤها `backfill_activity.py` فقط (لا المسجّل
  الدوريّ)، وتبقى مجموعة الجمع الأماميّة في `signal_events` نقيّة.
- `token_class` — تصنيف الأصل لكل عملة (`meme`/`major`/`priced`/`stable`): fomo
  منصّة متعدّدة الأصول، والأرشيف يحوي BTC وETH وSOL وذهباً وأسهماً مرمّزة
  (AAPL/MSTR/HOOD/INTC/META/SNDK/MU). مشتقّ بالكامل من المشاهدات عبر
  `classify_tokens.py` ⇒ يُعاد بناؤه متى شئنا. **أي تحليل أو تدريب يقيّد
  بـ`asset_class='meme'`** (PLAN §2.1) — التصنيف للفصل لا للحذف.
- `social_fetch_state` — حالة آخر سحب اجتماعيّ (تقود التناوب).
- `token_holders` — تركّز الملكية من **مصدرين يقيسان شيئين مختلفين** (لا تخلطهما):
  `source='token_details'` يعطي `top10_pct`/`holder_count` من السلسلة، و
  `source='hodlers_top'` يعطي تموضع **حشد منصّة fomo** (قيمة · مَن تحت الماء ·
  وسيط مدّة الحمل · حضور المطوّر) بلا أي نسبة عرض. صفّ لكل (عملة، مصدر، ختم).
- `holders_fetch_state` — حالة آخر سحب حائزين (تقود التناوب، خطوة 2.42).
- `outcomes` — النتائج/labels: يملؤها **الموسِّم** بعد نضج النافذة (انظر أدناه).
- `phase1_watch_outcomes` — **واجهة التحليل الآمنة**: تعرض فقط نتائج watch ذات
  `design_version>=3` و`analysis_eligible=1`. الضابطة القديمة حُذفت من قاعدة
  التشغيل؛ لا تستعمل `outcomes WHERE kind='watch'` مباشرةً في أي مقارنة جديدة.
- `meta` — حالة التشغيل، عدّادات، آخر خطأ لكل مصدر، طزاجة الـ feed.

تصميم المقارنة الحالي `v3` يشترط كوناً واحداً للطرفين: لا تُقبل نافذة إشارة
للمقارنة إلا إذا ظهرت العملة في `trending/verified` في دورة قبولها، ويُحفظ
`admission_source` وسعر القائمة نفسه. الضابطة تُقبل فقط في دورات قبلت إشارة
مقارنة، وتُطابق تراكمياً الشبكة والمصدر. بيانات `v2` السابقة معزولة وغير مؤهلة.

## الطبقة الاجتماعية

عملات الميم تحرّكها الحشود، وكنّا نسجّل السعر والحجم والحائزين ولا نسجّل **من
يتحدّث عنها**. جدول `token_social` يلتقط من `GET /feed/token/thesis` لكل عملة
مراقَبة كل ~30 دقيقة (`SOCIAL_PER_CYCLE`, `SOCIAL_REFRESH_SECONDS`).

### فخّ التشبّع (مُصلَح — لا تُعِده)

الاستجابة تعيد **100 عنصر كحدّ أقصى** بينما `responseObject.count` يحمل العدد
الحقيقي — شوهد **10,549** أطروحة لعملة واحدة. عدّ العناصر وحده يتشبّع، فتبدو
عملة فيها 10,549 أطروحة مطابقةً لعملة فيها 100 بالضبط، وهو إهدار لأقوى تمييز
في هذه الطبقة. لذلك:

| عمود | معناه |
|---|---|
| `thesis_total` | العدد **الحقيقي** من المغلّف — استعمله للمقارنة |
| `thesis_sampled` | كم عنصراً رأينا فعلاً (≤100) |
| `has_next_page` | هل العيّنة مبتورة |
| `thesis_likes/replies/authors/holder_authors` | محسوبة على **العيّنة** لا الكلّ |

لا تقارن `thesis_likes` بـ`thesis_total` كأنّهما من المقياس نفسه.

`thesis_authors` يعدّ الكتّاب المميّزين لا الأطروحات: عشر أطروحات من شخص واحد
ليست زخماً اجتماعياً. و`holder_authors` من يملك حصّة فعلاً (`equity>0`).

**الصمت يُسجَّل ولا يُتخطّى**: عملة بلا نقاش تُكتب بأصفار، وسلسلة الأصفار ثمّ
الارتفاع المفاجئ هي بالضبط ما نريد التقاطه. ولهذا لا تُستبعد العملة الصامتة من
الدورة (بخلاف الشموع، حيث `no_data` المتكرّر يُستبعد).

## حقول حجم الصفقة

`large_buy` كانت معطوبة: الحجم لم يكن يُستخرج إطلاقاً، فصفقة بـ 995$ وأخرى بـ
172,169$ متطابقتان أمام النموذج — رغم أنّ **وسيط ما تسمّيه fomo «شراء كبير» هو
3,448$ فقط**. الأعمدة الآن: `size_usd` (حجم المركز بعد الشراء)، `in_amount` (ما
دُفع فعلاً)، `in_token_address`، `out_amount`، `token_amount`، `realized_pnl_usd`.
الفرق بين `size_usd` و`in_amount` يميّز «أضاف 3آلاف إلى مركز 42ألف» عن «دخل
بـ45ألف دفعة».

رُحِّل الأرشيف كلّه رجعياً من `raw_json` عبر `backfill_sizes.py`.

## الموسِّم (labeler) — عملية FomoLabeler المنفصلة

يملأ جدول `outcomes` من الشموع **بعد اكتمال نافذة الـ48 ساعة + هامش 15 دقيقة**.
عملية منفصلة عن المسجّل مبدئياً: المسجّل يسجّل خاماً فقط، وأي حساب لقيمة مشتقّة
من المستقبل وقت التسجيل تسرّبٌ. الملفّات: `labeler.py` (منطق خالص قابل
للاختبار) + `run_labeler.py` (حلقة كل 15 دقيقة، نمط run_recorder).

### المفتاح (kind, key) — لماذا تغيّر

القديم `(token, entry_ts)` كان سيتصادم حتماً: 201 إشارة على عملة واحدة. الآن:
- `kind='signal'`, key = معرّف الإشارة — **صفّ تدريب لكل إشارة**.
- `kind='watch'`, key = `token:network:first_seen` — لكل دخول مراقبة **بما فيها
  الضابطة** — لمقارنة الإشارة/الضابطة على نوافذ مكتملة لا جارية.

### التعريفات (كلّها في `compute_labels` — دالة خالصة)

| الحقل | التعريف |
|---|---|
| `entry_px` | إغلاق **أوّل شمعة عند/بعد** لحظة الإشارة (تأخّر ≤30د وإلّا `no_entry`) |
| `max_gain_Xh` | أقصى `high` في الشموع **التالية بعد شمعة الدخول حصراً** ÷ الدخول −1 |
| `max_drawdown_48h` | أدنى `low` في النافذة ÷ الدخول −1 |
| `final_return_48h` | إغلاق آخر شمعة في النافذة ÷ الدخول −1 |
| `is_rug` | العائد النهائي ≤ −90% |
| `bars_truncated` | السلسلة انتهت قبل نهاية النافذة بأكثر من ساعة — **موت العملة إشارة لا نقص** |
| `is_independent` | أوّل إشارة للعملة أو فجوة ≥30د عن سابقتها (69% من الإشارات <5د — تكرار زائف يُعلَّم لا يُحذف) |
| `split` | train/val/test (70/10/20) بتجزئة **عنوان العملة** — كل إشارات العملة في قسم واحد، وإلّا حفظ النموذجُ العملات وصار التحقّق وهماً |

قمّة شمعة الدخول نفسها **لا تُحسب مكسباً** (قد تسبق التنفيذ الافتراضي)، وشموع
ما قبل الإشارة لا تدخل الحساب إطلاقاً (تسرّب معكوس).

### الإدارة

```powershell
Get-ScheduledTask FomoLabeler | Select State
Get-Content recorder\labeler.log -Tail 5      # يُكتب فقط حين يوسم شيئاً
py run_labeler.py 1                            # دورة يدوية واحدة
```

## مستخرج الميزات (`features.py` + `build_training_rows.py`)

يحوّل الأرشيف المبعثر إلى **جدول تدريب واحد**: صفّ لكل قرار، عمود لكل ميزة
معلومة **عند t=0 أو قبلها**، والليبل ملصوقاً من `outcomes`.

**القانون الواحد**: كل استعلام مقيَّد زمنياً صراحةً. اختبارات تزرع بيانات بعد t0
(أطروحة · شمعة · لقطة سوق · إشارة · عملة جديدة للمُنشئ · **لقطة حائزين** ·
**إدراج منصّة**) وتتأكّد أنّ الصفّ **لا يتغيّر** — هذه الاختبارات خطّ الدفاع
الوحيد ضدّ دقّة وهمية لا تُكتشف إلّا بالمال. ومعها حرس بنيويّ: لا عمود ليبل في
الميزات، ولا ميزة باسم likes.

**120 ميزة في 9 عائلات**: الحدث (حجم · قيمة سوقية · رتبة متصدّر · تركيبة الكتلة ·
متوسّط تكلفة المشتري · نصّ الرمز) · ثوابت العملة وبصمة المُنشئ **وشرعيتها
الخارجية** (عدد المنصّات · إدراج · وجود CMC · طول الوصف · بانر) · **تركّز
الملكية وتموضع حشد المنصّة** (`chain_top10_pct` · `chain_holder_count` ·
`platform_penetration` نفاذ المنصّة في الحائزين · `platform_underwater_ratio` مَن
تحت الماء · وسيط مدّة الحمل · حضور المطوّر) · **الزخم
الاجتماعيّ من مصدرين** (عدّ تاريخيّ من `token_thesis` + **العدد الحقيقيّ
وكتّاب مالكون وفروق الزخم** من لقطة `token_social` قبل t0) · **مسار السعر
والحجم قبل الإشارة** (عوائد 1س/4س/24س/7ي · تذبذب · `flat_ratio_24h` للهدوء قبل
الانفجار · انفجار الحجم `vol_surge_1h` · البعد عن القمّة) · **لقطة السوق بنوافذها
القصيرة** (change/volume/txn عند 1س/4س + دوران الحوض ونسبة التعويم) · النظام
السوقيّ (SOL/ETH) · كثافة الأحداث من المصدرين.

**عائلة الحائزين فارغة الآن، وهذا صحيح لا معطوب**: جمع الحائزين بدأ
2026-08-09، ونافذة النضج 48 ساعة تعني أنّ أحدث نتيجة ناضجة عند t0 =
2026-08-07 — أي **قبل** أوّل لقطة حائزين. فالقيد الزمنيّ يُرجع NULL بحقّ، ولا
يجوز «إصلاحه» بلصق لقطة اليوم على قرار الأمس (ذلك تسرّب مستقبل صريح). تبدأ
العائلة بالامتلاء تلقائياً مع نضوج نتائج ما بعد 2026-08-11. الميزات نفسها
مُتحقَّق منها على بيانات حيّة (6 عملات · 12 صفّاً · صفر أخطاء).

**قطعة واحدة للتدريب والحيّ**: نفس `build_features` ستحسب صفّ الإشارة الحيّة —
اختلاف الحساب بين التدريب والتشغيل (train/serve skew) يهدم النموذج بصمت.

واجهة التدريب الآمنة هي `model_training_rows`، لا `training_rows` الخام. الأولى
تفرض تلقائياً: `kind='signal'`, `is_live=1`, `asset_class='meme'`, `status='ok'`,
`is_independent=1`, و`feature_version>=2`. الإصدار الثاني يمنع الشمعة المفتوحة
وقت القرار والماكرو الساعي المفتوح، ويمنع `token_static` المسجل بعد `t0`.

**يعمل آلياً منذ 2026-08-11** عبر المهمّة المجدولة `FomoBuildRows`
(`run_build_rows.py`، AtLogOn، دورة كل ساعة من `BUILD_ROWS_INTERVAL_SECONDS`).
كانت هذه المرحلة الوحيدة اليدويّة في الأنبوب، فكان `training_rows` يتجمّد على
إصدار ميزات قديم بلا أيّ إنذار بينما تتراكم النتائج الموسومة.

الدورة تستدعي البناء التزايديّ نفسه على دفعات حتى `BUILD_ROWS_MAX_PER_CYCLE`
(4000) — السقف متعمَّد: رفع `FEATURE_VERSION` يجعل ~53 ألف صفّ pending دفعةً
واحدة، والقضم على دورات يجنّب مزاحمة المسجّل والموسِّم على قفل الكتابة ~90 دقيقة.
**لا `--rebuild` في المسار المجدول أبداً** (حذفٌ آليّ بلا يد بشرية).

```powershell
py run_build_rows.py --check-config      # تحقّق قبل التسجيل
py run_build_rows.py 1                   # دورة واحدة يدوياً
Get-ScheduledTask FomoBuildRows | Select State
Start-ScheduledTask -TaskName FomoBuildRows
```

سجلّاته `build_rows.log` (يُكتب فقط عند بناء فعليّ) و`build_rows_boot.log`،
ونبضته `build_rows_last_run_at` / `build_rows_last_stats` في `meta`.

التشغيل اليدويّ يبقى متاحاً للتقارير والحالات الخاصّة:

```powershell
py classify_tokens.py                   # حدِّث التصنيف أوّلاً (asset_class للصفّ)
py build_training_rows.py --dry-run     # تقرير التغطية بلا كتابة
py build_training_rows.py               # بناء تزايديّ
py build_training_rows.py --model-candidates-only  # الحيّ المستقل فقط (تحليل سريع)
py build_training_rows.py --rebuild     # يمسح الجدول أوّلاً — لا تحتاجه لترقية إصدار
```

**رفع `feature_version` لا يحتاج `--rebuild`**: المفتاح الأساسيّ `(kind, key)`
والإدخال `INSERT OR REPLACE`، فالبناء التزايديّ العاديّ يلتقط صفوف الإصدار
الأقدم ويستبدلها **في مكانها** — مقيس: العدد ثبت على 20,303 صفّاً بينما ارتفع
عدّاد v4. أمّا `--rebuild` فيبدأ بـ`DELETE FROM training_rows`، فاحفظه لحالة
واحدة: إخراج صفوف لم تعد مؤهّلة. الفرق ليس تجميلياً — مسح الجدول ثمّ تعثّر
البناء في المنتصف يترك القاعدة أفقر مما بدأت.

**اقرأ تقرير التغطية بعد كل بناء**: عمود فارغ 100% إمّا فراغ منبع أو عطب عندنا.
ثلاثة دروس مقيسة من التدقيق:

1. `token_age_h` كان فارغاً كليّاً لأنّ fomo تخزّن `token_created_at` **epoch
   رقمياً** لا ISO (أُصلح؛ التغطية 96.9%).
2. **عمود ممتلئ قد يكون خاطئاً**: كنّا نستعمل عدّادات الشراء/البيع (تغطية 24%)
   ونُهمل `change_*`/`volume_*`/`txn_*` (تغطية **100%** على 588 ألف لقطة).
3. **عمود صحيح قد يكون مشبَّعاً**: `token_thesis` عيّنة تتوقّف عند ~400، والحقيقة
   في `token_social.thesis_total` (بلغت 27,559) — فأصبح العدّ معلَّماً بـ
   `thesis_counted_capped` ومعه العدد الحقيقيّ.

و`mintable`/`freezable`/`top10_holders_pct` فراغ منبع حقيقيّ لا عطب. وعمر سالب
مستحيل (ختم إدراج لا إنشاء) يُترك None لا رقماً سالباً يتعلّمه النموذج.

## تحليل أنماط الصعود والضوضاء (`pattern_analysis.py`)

يستخرج قواعد قابلة للتفسير من `train` فقط، ثم يقيسها على `val` و`test` وعلى
الأيام منفصلة، ويحسب فواصل الثقة بإعادة أخذ العينات على مستوى **العملة**. يفصل
بين لمس +20% خلال 24 ساعة، والإغلاق النهائي بعد 48 ساعة، ومحاكاة خروج تمشي على
الشموع بترتيبها (هدف +20%، وقف −30%، خروج 24 ساعة، تكلفة دورة 2%).

```powershell
py pattern_analysis.py
# الناتج: docs/pattern-analysis-YYYY-MM-DD.md
```

بعد قراءة تقرير الاختبار تصبح عيّنة `test` الحالية مستهلكة؛ لا تُعدّل القواعد
ثم تعاود تسميتها اختباراً مستقلاً. ثبّت القواعد وانتظر بيانات زمنية جديدة.

## تصنيف الأصول (`classify_tokens.py`)

fomo **متعدّدة الأصول**: الأرشيف يحوي BTC ($1.27T) وETH وSOL (103 إشارة) وذهب
PAXG وأسهماً مرمّزة (AAPL $339 · SNDK $1013 · MU $958 · MSTR · HOOD · INTC ·
META) وعملات مستقرّة. التصنيف المقيس: **686 ميم · 21 كبير · 14 مسعَّر · 4 مستقرّة**
من 725 عملة.

القواعد بترتيبها (`extract.classify_asset`، عتباتها في config):

1. `stable` — كل المشاهدات داخل [0.95, 1.05].
2. `major` — قيمة سوقية > $1B، **بشرط مصداقيتها**: فوق $5T تُتجاهل (شوهد $69T
   لعملة بـ$0.0888 = سعر × معروض خرافيّ) ويُحكم بالسعر.
3. `priced` — سعر > $5: الأسهم والسلع المرمّزة. **القيمة السوقية لا تكشفها**
   (AAPL بـ$1.36M فقط لأنّ المرمَّز جزء ضئيل من السهم) — السعر وحده يكشفها.
4. اسم معروف (BTC/ETH/XRP…) **بشرط قيمة معتبرة** — الميمات تنتحل الرموز
   (مقيس: «BTC» بـ$3.5M و«SOL» بـ$4.9M — تبقى ميمات).
5. `meme` — الباقي، وهو هدف المشروع.

```powershell
py classify_tokens.py --dry-run   # التوزيع + كل غير الميمية بأسبابها
py classify_tokens.py             # كتابة/تحديث token_class
```

**الاستعمال الإلزاميّ**: أيّ تحليل/تدريب يقيّد بـ`asset_class='meme'`
(PLAN §2.1). وقد أثبت الفحص أنّ أرقام الرجعيّ لم تتأثّر (5 صفوف من 487) لكنّ
الأماميّ ملوَّث أكثر (SOL وحده 22 نتيجة).

## سلامة الشموع — قيم مستحيلة من المنبع

fomo تعيد أحياناً أسعاراً مستحيلة في `getBarsNew`. حالتان مقيستان (2026-07-30،
**مؤكَّدتان بإعادة سحب حيّة ⇒ تشوّه دائم في المنبع لا خطأ نقل عندنا**):

| الموضع | القيمة | السياق |
|---|---|---|
| `h` | **2,626,092.02** | إغلاق الشمعة 0.0219 وجاراها 0.0040/0.0202 (×119 مليون) |
| `c` | **12,052.5** | بين إغلاقين ≈0.0004 — ثمّ يعود فوراً |

الأثر قبل الإصلاح: `max_gain` بلغ **+3.78 مليار%** في 4 صفوف نتائج، ولوحة
القيادة عرضت **+62,570,743,609%** لعملة صعودها الحقيقيّ +1000%.

**الحكم: السعر متّصل في المجمّع** (`extract.bar_context_flags`) — إغلاق الشمعة
هو افتتاح تاليتها، فقيمة تتجاوز **كلا** جاريها بـ×10 (`BAR_WICK_MAX_RATIO`) ولا
تستمرّ = تشوّه لا سعر. الجار مرجعٌ لا الجسم، لأنّ التشوّه يصيب الإغلاق نفسه
فيتمدّد الجسم ويبدو الذيل معقولاً. واشتراط جارٍ على **الطرفين** يمنع وسم بداية
اتجاه حقيقيّ (قفزة استمرّت = سعر ✅ · انهيار استمرّ = rug صحيح ✅).

- تُعلَّم `h_suspect`/`l_suspect`/`c_suspect` وتُستبعد من الحساب فقط —
  **لا تُصلَح ولا تُحذف** (الخام مقدَّس، والعلم قابل لإعادة الحساب منه دائماً).
- الموسِّم: القمّة/القاع من شموع سليمة، والعائد النهائي وسعر الدخول من **إغلاق
  سليم** (إغلاق دخول مشوّه يُتخطّى إلى أوّل سليم)، و`outcomes.suspect_bars`
  يعدّ المشوّهة في النافذة فتُعرف الصفوف المتأثّرة ولا تُقرأ كأنّها نقيّة.
- الحكم يحتاج الشمعة التالية، فـ`db.recompute_bar_flags` يُستدعى بعد كل إدراج
  (دورة الشموع، الماكرو، استرجاع شموع الرجعيّ).
- العتبة ×10 مقيسة على 652,342 شمعة: 132 فقط تتجاوز ×2، و39 تتجاوز ×10،
  و22 تتجاوز ×1000 — الذيول المشروعة تنتهي عملياً دون ×2.

```powershell
py backfill_bar_flags.py --dry-run   # إعادة حساب الأعلام للشموع القديمة (بلا شبكة)
py backfill_bar_flags.py
py relabel_suspect.py --dry-run      # نتائج نافذتها مشوّهة (تُحذف لتُعاد)
py relabel_suspect.py                # + نسخة القيم القديمة في ملفّ JSON
py run_labeler.py 1                  # إعادة الحساب فوراً بدل انتظار الدورة
```

## استرجاع تاريخ النشاط (`backfill_activity.py`)

الـ `/feed` الأماميّ «أحدث فقط» (خمسة معاملات ترقيم مُتجاهَلة — مُثبت حيّاً)،
لكنّ `GET /feed/tradingActivity?lastId=` — الذي تستعمله الـ SPA نفسها — يصفّح
رجوعاً حتى **نهاية التاريخ (2025-10-22)**. السحب الأول (2026-07-28) جلب الكون
كاملاً: **1151 حدثاً**، منها **819 `multi_user_buy` عبر 408 عملة على 9 أشهر** —
عيّنة الأطروحة المركزية رجعياً بدل انتظار 71 يوماً. التطابق مع `signal_events`
(43 حدثاً) إثبات أنّه الكون نفسه؛ الدمج عند النمذجة بالمعرّف.

**ما لا يغطّيه**: فيض `large_buy` (~1138/يوم) ليس في هذا المسار — يبقى حصريّاً
للجمع الأماميّ. وأحداث `swap_*` الفردية عمقها يومان فقط. ورتبة المتصدّر وقت
الحدث مفقودة تاريخياً (تُشتقّ من أرشيف الصدارة للمستقبل فقط).

مشيّاط قابل للاستئناف (نقطة في `meta`)، إعادة محاولة ×3 عند العابر، idempotent:

```powershell
py backfill_activity.py --pages 30              # يتوقّف تلقائياً عند نهاية التاريخ
py backfill_activity.py --until 2026-06-01      # أو حتى تاريخ
py backfill_activity.py --pages 5 --dry-run     # قياس بلا كتابة
py backfill_activity.py --reset                 # من الأحدث مجدّداً
```

**توسيم الرجعيّ** (مُنفَّذ): `backfill_activity_bars.py` يسحب شموع 5د حول أزمنة
أحداث كل عملة ([ts−1س, ts+48س+هامش] بعناقيد مدمجة ≤72س/نداء)، ويسجّل حالته في
`activity_bars_state`. الموسِّم يوسم `activity_events` بنفس `compute_labels`
ونفس علم الاستقلال — لكن **فقط بعد status='ok' لعملته** (بوّابة في
`activities_pending_label`) حتى لا يُكتب no_entry أبديّ قبل وصول الشموع.
العملة بلا سلسلة لدى fomo تُوسَم `no_data` وتُستبعد بعد 3 محاولات — **نسبتها
هي انحياز البقاء في الرجعيّ** (العملات الميّتة = حالات rug المفقودة حتماً)،
تُقرأ في أي استنتاج ولا تُخفى. النتائج تدخل `outcomes` بـ`kind='activity'`
(ولا ضابطة رجعية ممكنة: الكون غير المُشار إليه تاريخياً مجهول — قيد على أي
مقارنة ضابطة تشملها).

## محاكي قواعد الخروج (`exit_sim.py`)

**الفرضية التي يختبرها** (فرضية لا نتيجة): اصطياد القمّة مستحيل سلفاً، فربّما
كان هدف ثابت متواضع أقرب للتحقيق. هذا **سؤال مفتوح** والمحاكي أداة اختباره.

> ⚠️ **لا تبنِ قراراً على مخرجاته الحالية.** التشغيلات حتى الآن على عيّنة من
> عشرات الصفقات بمتابعة مبتورة، **بلا نافذة 48 ساعة واحدة مكتملة وبلا مقارنة
> ضابطة**. (الأداة تطبع حالة نضجها الفعلية قبل كل جدول — اقرأها هناك لا هنا.)
> الأهداف البعيدة مُبخَّسة بنيوياً (الوقت لم يمضِ)، والفرق المرصود قد يكون خاصية
> سوق تلك الساعات لا خاصية الاستراتيجية. الأرقام تتغيّر مع كل تشغيل.

**متى تصير المخرجات ذات معنى**: بعد اكتمال النوافذ (المرحلة 0) ونضج المجموعة
الضابطة (المرحلة 1). شغّل المحاكي **على المجموعتين وقارن** — إن تفوّق الجني
الثابت على الضابطة أيضاً فالأثر حقيقيّ، وإلّا فهو خاصية سوق لا استراتيجية.

### لماذا نمشي على الشموع ولا نقرأ أعمدة `outcomes`

`max_gain_48h` و`max_drawdown_48h` **لا تحملان ترتيبهما**. عملة هبطت −40% ثمّ
صعدت +50%: مع وقف −30% أنت خارج بخسارة، وبلا وقف أنت رابح — والعمودان متطابقان
في الحالتين. **المسار هو النتيجة**، ولهذا يمشي المحاكي على الشموع (مُثبَّت
باختبار: مساران بنفس القمّة والقاع ونتيجتان معكوستان).

### الافتراض المتحفّظ داخل الشمعة

الشمعة تعطي `high` و`low` بلا ترتيب زمنيّ. إن لُمس الهدف والوقف في الشمعة نفسها
نفترض **الوقف أوّلاً** — أسوأ الاحتمالين، فالنتائج حدّ أدنى لا مبالغة.
(`optimistic_same_candle=True` يقلبه لقياس حجم الأثر فقط، لا للتقرير.)

### الاختبار الحاسم: التكلفة

الأفضلية النظرية بلا معنى إن ابتلعها الانزلاق. `breakeven_cost` يحسب أقصى تكلفة
دورة تبقى معها القاعدة رابحة — والنتيجة الحالية **1.9% – 3.2%** حسب القاعدة،
بينما **ثلث العملات سيولتها دون 50 ألف دولار**. القاعدة التي تعادلها دون ~3%
ليست قابلة للتنفيذ عملياً مهما بدت جميلة على الورق.

### الاستعمال

```powershell
py run_exit_sim.py                # المجموعة الافتراضية (الجمع الأماميّ) + نقاط التعادل
py run_exit_sim.py --cost 0.02    # بتكلفة 2% لكل دورة
py run_exit_sim.py --all-signals  # كل الإشارات لا أوّل إشارة لكل عملة
py run_exit_sim.py --source activity --cost 0.02
                                  # الرجعيّ: multi_user_buy من activity_events
                                  # (382 صفقة أوّل-لكل-عملة — بلا ضابطة ممكنة)
```

يقرأ فقط — آمن مع المسجّل العامل. افتراضياً **أوّل إشارة لكل عملة** (14.2 إشارة
لكل عملة ووسيط الفاصل 1.1 دقيقة؛ محاكاتها كصفقات منفصلة تضخّم النتيجة بلا معنى).

⚠️ الأرقام أعلاه على **نوافذ غير مكتملة** (أقصى متابعة ~25 ساعة من 48)، فالأهداف
البعيدة (+50% وما فوق) **مُبخَّسة**. ولا مقارنة ضابطة بعد.

## ما استُرجع رجعياً وما ضاع نهائياً

مبدأ «الخام دائماً» في التصميم الأصلي هو ما جعل الاسترجاع ممكناً: الحقول لم تكن
ضائعة بل **غير مستخرَجة**.

| البيانات | الحالة | الأداة |
|---|---|---|
| حقول حجم الصفقة | ✅ مُسترجَعة 100% | `backfill_sizes.py` |
| الشموع | ✅ أعمق من الأرشيف نفسه (تعود لـ2025-11) | تلقائيّ |
| عدد الأطروحات التاريخي | ✅ مُسترجَع عبر ترقيم الصفحات | `backfill_thesis.py` |
| مجموعة ضابطة لفترة الإشارات | ✅ مبنيّة من `snapshots` | `seed_control_retro.py` |
| **إعجابات/ردود لحظة الإشارة** | ❌ **ضائعة نهائياً** | — |

**الخسارة الوحيدة الحقيقية**: fomo تعطي عدّاد الإعجابات **الحاليّ** فقط ولا سجلّ
تاريخياً له. أطروحة لها 50 إعجاباً اليوم — كم كان لها لحظة الإشارة؟ لا سبيل.
لذلك `token_thesis.fetched_at` مسجَّل: هو زمن قياس الإعجابات، ولا تُقرأ
`num_likes` كأنّها قيمة وقت الكتابة. من الآن فصاعداً السلسلة الزمنية في
`token_social` تحلّ المشكلة للمستقبل.

`token_thesis` (صفّ لكل أطروحة بختم كتابتها) يجعل «كم أطروحة كانت لحظة الإشارة»
استعلاماً بسيطاً — `db.thesis_count_before(token, at)`.

## طزاجة الـ feed المصدر

`meta.last_feed_event_at` يحمل ختم أحدث حدث. **المسجّل قد يعمل بلا خطأ بينما
feed فomo متجمّد** — شوهد متجمّداً 3 ساعات بينما كل الدورات تنجح. بلا هذا الختم
يبدو الأرشيف «سوقاً هادئاً» وهو انقطاع مصدر، والفرق حاسم لأي تحليل زمنيّ.
اللوحة تعرضه في الشريط العلوي.

## المجموعة الضابطة (الصنف السالب)

**المشكلة**: كانت كل عملة لها سلسلة سعرية قد دخلت بإشارة (90 من 90). فالنموذج
يستطيع تعلّم «أيّ عملة مُشار إليها ترتفع أكثر»، لكنّه **لا يستطيع أبداً** الإجابة
عن «هل الإشارة تعني شيئاً أصلاً». قد تجد 41% من إشاراتك رابحة، ثمّ يتّضح أنّ 41%
من السوق رابح في تلك المدّة — فالإشارة لا تضيف شيئاً، ولا سبيل لاكتشاف ذلك.

**الحلّ**: عملات تدخل المراقبة **بالاختيار العشوائي لا بإشارة**، بعمود
`watchlist.is_control = 1` و`source = 'control'`، وتُسجَّل بنفس النافذة (48س) ونفس
الشموع ونفس الـ ticks. الضبط في `config.py`: `CONTROL_GROUP_SIZE` (40)،
`CONTROL_PER_CYCLE` (2)، `CONTROL_WATCH_HOURS` (48).

### شروط صلاحية المقارنة (كلّها مقصودة، في `admit_control_sample`)

- **عشوائيّ لا ترتيبيّ**: الأخذ من رأس قائمة الرواج يختار الأعلى حجماً، فيصير
  الفرق عن المُشار إليها **فرقَ حجمٍ لا فرقَ إشارة**.
- **بلا نظر إلى المستقبل**: الاختيار يعتمد على المعروف لحظتَه فقط.
- **يُستبعد كل ما أُشير إليه** ولو لم يدخل المراقبة (`signalled_tokens`) — وإلّا
  لم يعد ضابطاً.
- **بالتقسيط**: عملتان في الدورة. أخذ الأربعين دفعةً واحدة يجعلها كلّها عيّنة من
  لحظة سوقية واحدة، فيختلط أثر الإشارة بأثر تلك اللحظة.
- **الترقية باتجاه واحد**: عملة ضابطة وردتها إشارة تصير مُشاراً إليها بنافذة
  جديدة؛ والعكس ممنوع (`admit_control` لا تلمس صفّاً موجوداً أبداً).
- **السقف منفصل**: `WATCHLIST_CAP` يخصّ المُشار إليها وحدها فلا تزاحمها الضابطة.

اللوحة تعرض المقارنة في «الإشارة مقابل المجموعة الضابطة» مع تنبيه صريح ما دامت
العيّنة أصغر من 20 لكل جانب.

## الشموع (OHLCV) — مصدر الحقيقة السعرية

`market_ticks` **لا تكفي للتوسيم**. مصدرها قائمتا `trending`/`verified`، وهما
قائمتان منسَّقتان من ~46 و~37 عملة لا خلاصة أسعار لكل عملة. القياس على الأرشيف
الحيّ: **21 من 84 عملة مراقَبة (25%) لم تحصل على أي نقطة سعرية إطلاقاً**، والباقي
تغطيته 52–75% من الدقائق فقط. أي أنّ ربع العيّنات كان مستحيل التوسيم.

`token_bars` يسدّ ذلك عبر `POST /proxy/getBarsNew`. القياس بعد التفعيل: **تغطية
~100%** من شبكة الخمس دقائق، وتاريخ يمتدّ إلى **11 يوماً قبل** بدء المسجّل نفسه.

### تفاصيل حرجة مؤكَّدة حيّاً (2026-07-26)

- `symbol` **يجب** أن يكون `"<address>:<networkId>"`. العنوان المجرّد يجعل خادم
  fomo يرمي **502 من Cloudflare** — يبدو عطلاً في fomo وهو في الحقيقة طلب مشوّه.
- `from` و`to` **إلزاميان** (بدونهما `400 "body.from - Required"`).
- سقف النداء الواحد **900 شمعة**؛ عند 5 دقائق = 75 ساعة، تغطّي نافذة 48 ساعة.
- **توفّر الشموع يتآكل**: عملة أعادت 192 شمعة ثمّ بعد 15 دقيقة `404 No OHLCV data
  available`. لذلك **لا تؤجّل السحب** اعتماداً على أنّ التاريخ سيبقى متاحاً.
  العملة التي تُردّ `no_data` ثلاث مرّات تُستبعد (`BARS_MAX_NO_DATA_ATTEMPTS`).

### الجدولة الدوّارة

سحب 84 عملة كل دقيقة يُثقل الحلقة ويُغرق fomo. بدلاً من ذلك تأخذ كل دورة شريحة
`BARS_PER_CYCLE` (=6) من العملات **الأقدم سحباً أوّلاً**، فيكتمل المسح عبر ~14
دورة (~14 دقيقة) وتُحدَّث كل عملة كل `BARS_REFRESH_SECONDS` (=900). العملة التي لم
تُسحب قطّ تسبق الجميع، فيتمّ backfill الدخول فوراً.

نطلب دائماً النافذة الكاملة منذ (أول ظهور − ساعتَي سياق) لا منذ آخر شمعة: الشمعة
الأخيرة تكون قيد التكوّن فتُراجَع (الإدراج `OR REPLACE`)، والسحب الكامل يشفي أي
ثغرة سابقة تلقائياً.

## تخزين الخام (ضغط zlib)

أعمدة `raw_json` تحمل **BLOB مضغوطاً بـ zlib**، لا نصّاً. اقرأها دائماً عبر
`db.decode_raw()` — يفكّ الضغط ويقبل أيضاً الصفوف القديمة المكتوبة نصّاً:

```python
from db import decode_raw
raw = decode_raw(row["raw_json"])   # ← لا json.loads(row["raw_json"])
```

**لماذا**: لقطة كاملة من `feed`+`trending`+`verified` تُكتب كل دقيقة (~310 KB
لكلٍّ)، فبلغت القاعدة 720 MB خلال 20 ساعة بنموّ ~1.3 GB يومياً و`snapshots` وحدها
80% منها. الضغط **بلا خسارة** ويخفض ذلك إلى ~220 MB يومياً. المفتاح
`meta.raw_encoding = "zlib"` يوثّق الترميز الفعّال.

الأرشيف الموجود رُحِّل مرّة واحدة عبر `migrate_compress.py` (739 MB → 184 MB).
الترحيل يتحقّق من كل صفّ قبل الكتابة وقابل للاستئناف؛ يتطلّب إيقاف المسجّل:

```powershell
Stop-ScheduledTask -TaskName FomoRecorder
py migrate_compress.py --dry-run   # تقدير المكسب فقط
py migrate_compress.py             # ترحيل + VACUUM
Start-ScheduledTask -TaskName FomoRecorder
```

### النسخ الاحتياطي الحيّ

`backup_db.py` يستعمل `VACUUM INTO` إلى ملف مرحلي محلي ولا يحتاج إيقاف المسجّل.
هذا يثبّت snapshot ولا يعيد نسخ 5.5 GB عند كل كتابة WAL، ثم يفحصه
بـ`PRAGMA quick_check` وينشره ذرياً ويحتفظ بآخر ثلاث نسخ فقط.
المهمّة اليومية `FomoBackup` تُنشأ عبر:

```powershell
.\setup_backup_task.ps1   # الافتراضي: %OneDrive%\aoi-backups، يومياً 03:15
```

الوجهة يجب أن تكون خارج المشروع؛ اضبط `AOI_BACKUP_DIR` لقرص خارجي/مجلد مزامَن.

**الاحتفاظ**: `config.SNAPSHOT_RETENTION_DAYS = 0` (بلا حذف) افتراضياً — اللقطات
أرشيف إعادة الاشتقاق فحذفها قرار صريح. اضبطه على عدد أيام لتفعيل التقليم الدوري.
اللوحة تعرض «حجم القاعدة» و«نموّ يوميّ» لمراقبة ذلك.

## الملفات

| ملف | الدور |
|-----|------|
| `config.py` | مسارات، تواترات (60ث)، مدّة المراقبة (48h)، الحدّ (150)، أصول الماكرو، وصل `api/src` |
| `db.py` | المخطّط + WAL + إدراج idempotent + ضغط الخام + جدولة الشموع (بلا شبكة) |
| `migrate_compress.py` | ترحيل لمرّة واحدة: ضغط `raw_json` الموجود + VACUUM |
| `backup_db.py` / `setup_backup_task.ps1` | نسخة حيّة متحققة + مهمة يومية خارج المستودع |
| `extract.py` | تحويلات خالصة خام → صفوف (قابلة للاختبار بلا شبكة) + حكم سلامة الشموع (`bar_context_flags`) + تصنيف الأصول (`classify_asset`) |
| `leaderboard_cache.py` | صدارة المتصدّرين **الخام** لأربع مدد (all/24h/7d/30d): خريطة id→rank لكلٍّ منفصلة + خام كلٍّ للأرشفة الساعية في snapshots |
| `recorder.py` | الحلقة الرئيسية (`run_cycle` قابلة للاستدعاء مرّة أو تكراراً) |
| `labeler.py` / `run_labeler.py` | حساب النتائج (منطق خالص) + خدمة التوسيم كل 15 دقيقة |
| **`features.py`** | **مستخرج الميزات: 120 ميزة مقيَّدة بـt=0 — قطعة واحدة للتدريب والحيّ** |
| `build_training_rows.py` / `run_build_rows.py` | بناء `training_rows` من النتائج الموسومة + تقرير التغطية · خدمة البناء كل ساعة (`FomoBuildRows`) |
| `pattern_analysis.py` | قواعد صعود/ضوضاء مفسّرة + تحقق بالعملة واليوم + محاكاة خروج |
| `backtest_strategy.py` | باك تست كامل لأي قاعدة عتبات (افتراضياً نمط الزخم) على كل الأرشيف + محاكاة شموع + متانة التكلفة |
| `train_pipeline.py` | أنبوب التدريب النهائي: walk-forward زمني + فصل العملات + هدف من محاكاة الخروج + AUC/bootstrap بالعملة + فحص تسريب إجباري |
| `classify_tokens.py` | تصنيف الأصول → `token_class` (ميم/كبير/مسعَّر/مستقرّة) |
| `backfill_bar_flags.py` | إعادة حساب أعلام تشوّه الشموع من الخام |
| `backfill_extracted_fields.py` | اشتقاق أعمدة أُضيفت متأخّرة من `raw_json` المحفوظ — **صفر نداء شبكة**، ويستدعي دوالّ `extract` نفسها فلا ينجرف تعريفان |
| `relabel_suspect.py` | حذف نتائج نافذتها مشوّهة ليعيد الموسِّم حسابها (بنسخة احتياطية) |
| `backfill_activity.py` / `_bars.py` | استرجاع تاريخ tradingActivity + شموع أزمنته |
| `backfill_bars.py` | استرجاع شموع 1D لكل العملات لحساب ATH كامل وقابل للاستئناف |
| `backfill_training_ath.py` | ترقية ATH في صفوف التدريب القديمة بعد اكتمال التاريخ اليومي |
| `exit_sim.py` / `run_exit_sim.py` | محاكي قواعد الخروج (`--source activity` للرجعيّ) |
| `run_recorder.py` | نقطة إطلاق المهمّة المجدولة (نمط `serve.py`) |
| `tests/` | اختبارات على أشكال خام حقيقية ومنطق db (بلا شبكة) — منها **63 اختبار حرس** |

## التشغيل

يعتمد على وجود اعتماد صالح في ملف حالة الـ api (`api/.privy_state.json`) — يولّده
تشغيل خدمة الـ api. المسجّل يقرأ `session_token` منه مباشرة ولا يطبع قيمته أبداً.

```powershell
python run_recorder.py        # حلقة لا نهائية (كل 60 ثانية)
python run_recorder.py 1      # دورة واحدة (تحقّق حيّ)
```

## الاختبارات

```powershell
python -m pytest recorder/tests -q      # 371 اختباراً، بلا شبكة
```

أهمّها للنمذجة: `test_features.py` (**63 اختبار حرس** — يزرع بيانات بعد t0 ويثبّت
أنّها لا تظهر، ومنها حرس اللقطات الجديدة: حائزون وإدراج منصّة)،
`test_holders_cycle.py` (فصل المصدرين · نافذة التحديث · أرشفة الخام)،
`test_bar_integrity.py` (18 — تشوّه المنبع بأرقامه الحقيقية)،
`test_asset_class.py` (18 — انتحال الرموز والأسهم المرمّزة).

## الأعمدة الفارغة أو الثابتة — تصنيف مقيس (2026-08-09)

جرد كامل على **48,287 صفّ تدريب × 120 ميزة**. 33 عموداً بدت «معطوبة»، وليست
صنفاً واحداً. **لا تُصلح عموداً من هذه الجداول قبل قراءة سببه**: ثلاثة منها
صحيحة ولا يجوز ملؤها، والرابع وحده كان عطلاً.

### أ — يتعذّر جمعه: المصدر يرسل الحقل وقيمته ميتة

الحقل موجود في المغلّف الخام، ونقرأه، لكنّ fomo تضع فيه القيمة نفسها دائماً.
لا شيء نصلحه هنا — ولا شيء يتعلّمه النموذج منه.

| العمود | القياس على الخام | الحكم |
| --- | --- | --- |
| `social_replies` | `numReplies = 0` في 28,104/28,104 أطروحة، وكل `comment.parentId` فارغ | الردود لا تصل في هذا الـendpoint أصلاً |
| `are_top_traders` | `areTopTraders: false` حرفياً في الخام | ليس فراغاً بل جواب المصدر |
| `is_scam` | 0 في كل صفّ (39 مقيسة) | لا عملة موسومة احتيالاً في أرشيفنا |
| `platform_dev_holding` | `isDev = false` في 5,195/5,195 حائزاً | العلم غير مفعّل من المنبع |

وحقلان ميتان **لا نقرأهما** (موثّقان في `extract.py` كي لا يُستعملا لاحقاً):
`equity` صفر في 28,186/28,186، و`comment.reactions.counts.likeCount` صفر دائماً
لأنّه حالة **القارئ** لا العدّ العام — العدّ العام `comment.numLikes` (غير صفري
في 46.7%) وهو ما نقرأه فعلاً.

### ب — بلا تباين بحكم التعريف (لا عطل)

- `listed_on_exchange` = 1 في 45,857 صفّاً: fomo تعبّر عن «لا منصّات» بغياب
  المفتاح لا بصفر، ففرع `else 0` في `features.py` **غير قابل للوصول**. العدد
  نفسه (`exchanges_count`) يحمل المعلومة؛ العلم لا يحملها.
- `rank_le_50` = 1 دائماً: **سقف المصدر 50** (لا 200 كما كان مكتوباً هنا —
  `LEADERBOARD_SIZE` مُهمَل من المصدر)، فـ`buyers_best_rank` لا يتجاوز 50
  بحكم البناء لا بالصدفة. `rank_le_10` هو المميّز الحيّ (distinct=2).
  صدارات المدد **ترفع هذا السقف**: كلٌّ منها تعيد 100، فرتب
  `buyers_best_rank_24h/_7d/_30d` و`best_rank_any_period` مداها 1-100 —
  و`rank_le_50` يستعيد تباينه إن حُسب على `best_rank_any_period` لا على
  `buyers_best_rank` وحده.
- `minutes` = 15 دائماً: نافذة `multi_user_buy` ثابتة عند المصدر.

### ج — محجوب زمنياً: يُجمع الآن ولا يمكن أن يظهر بعد

عشرة أعمدة عائلة الحائزين (`top10_holders_pct` · `chain_top10_pct` ·
`chain_holder_count` · `holders_age_min` · `platform_holders` ·
`platform_penetration` · `platform_underwater_ratio` · `platform_value_usd` ·
`platform_median_hold_h` · `platform_dev_holding`) صفرها **ليس عطلاً**:

- الجمع سليم: `token_holders` فيه 5,196 صفّاً · 219 عملة · و219/219 حالة `ok`.
  التغطية داخل كل مصدر كاملة (`top10_pct` في 2,604/2,604 صفّ `token_details`،
  والعائلة المنصّية في 2,582/2,604 من `hodlers_top` ≈ 99%).
- لكنّ أقدم لقطة حائزين هي `2026-08-09T10:20`، وأحدث t0 **ناضج** هو
  `2026-08-07T20:32` — فجوة ~38 ساعة. اللقطة تُقرأ عند/قبل t0 حصراً، فلا صفّ
  ناضج اليوم يمكن أن يجد لقطة تسبقه.
- أوّل صفوف تحمل هذه الميزات تنضج نحو **2026-08-11** (t0 ≥ أوّل لقطة، زائد
  نافذة 48س وهامش). **ملؤها قبل ذلك تسريبُ مستقبل** — الرقم الوحيد المتاح
  قُيس بعد انتهاء النافذة.

نفس المنطق يفسّر التغطية الجزئية في `mintable`/`freezable` (3.3%): مقيسة
100% على سولانا حيث لها معنى، وNULL بحقّ على EVM (لا سلطة بهذا المعنى — FR-007).

### د — عطل حقيقي، أُصلح

عمودان فقط من الـ33 كانا خطأً برمجياً، وكلاهما من النوع نفسه: **المستخرِج يقرأ
حقلاً لا يرسله المصدر بالشكل المتوقَّع**، فيسقط صامتاً إلى قيمة موحّدة.

1. `mintable`/`freezable` — كانا NULL في 100%: المصدر يعيد **عنوان** السلطة لا
   قيمة منطقية. بعد التصحيح: سولانا 312/312 مقيسة (56 بسلطة سكّ قائمة).
2. `social_holder_authors`/`social_holder_ratio` — كانا 0 في 46,040 صفّاً:
   المستخرِج قرأ `equity` (الميت أعلاه) بدل `authorTrade.humanTokenAmount`.
   بعد التصحيح والتعبئة الرجعية (44,763 لقطة، **بلا نداء شبكة**): 99 قيمة
   مميّزة على مدى 0–100، وحائز واحد على الأقل في 95.2% من اللقطات، والنسبة
   موزّعة فعلاً (القمّة 30–60%، وذيل ~900 لقطة كل كتّابها مالكون).

**الدرس الذي يعمّم**: عمود ثابت على 0 أخطر من عمود NULL. الـNULL يعلن غيابه،
أمّا الصفر فيبدو قياساً ويدخل التدريب بوزن. حين يبدو عمود «معبّأً وبلا تباين»،
افحص **الخام** لا الجدول: كلا العطلين ظهر فور طباعة شكل المغلّف الحقيقي.

### لماذا لا تُحذف أعمدة الصنف (أ)؟

السؤال في محلّه: إن كان العمود ميتاً فلمَ يبقى؟ لأنّ «يبقى» تعني ثلاثة أشياء
مختلفة، ولكلٍّ منها حساب مخالف:

| الطبقة | القرار | السبب |
| --- | --- | --- |
| الخام (`raw_json`) | **لا يُحذف أبداً** | هو ما سمح بإصلاح `mintable` و`holder_authors` رجعيّاً بصفر نداء شبكة. المحذوف منه لا يُستعاد إلّا بإعادة جمع مستحيلة (النوافذ الزمنية مضت). |
| عمود الجدول | **يبقى** | هو المِجَسّ. عمود محذوف لا يُقاس، فلو بدأت fomo ترسل `numReplies` حقيقياً لما عرفنا. بقاؤه يكلّف بايتات ويشتري كشفاً. |
| مجموعة ميزات النموذج | **تدخل كلّها** | `FORBIDDEN` قائمة **منع** يدوية، وكل عمود لا يُذكر فيها صراحةً يصير ميزة. لا فلترة آليّة بعدها. |

**قرار صريح (2026-08-11): لا فلترة آليّة للأعمدة إطلاقاً.** كان
`prune_dead_features` يُسقط كل عمود بلا تباين، ويُسقط كل عمود قيمةُ نصفه الثابت
غائبة تماماً عن النصف الآخر بوصفه **علامة حِقبة**. حُذف بطلب صاحب المشروع
ولا يُعاد. الوسيلة الوحيدة لاستثناء عمود اليوم هي `FORBIDDEN` بيدٍ.

الثمن المقبول بهذا القرار — يُذكر لأنّه يقع فعلاً لا لأنّه احتمال:

- عمود يبدأ فارغاً ثمّ يُملأ (كل ما جُمع حديثاً: عائلة التدفّق، دمج المصادر،
  صدارات المدد) يصير NULL في الصفوف القديمة ومقيساً في الجديدة. فيقرأ منه
  النموذج **حِقبة الصفّ** لا حال العملة، ويرتفع AUC بلا أفضلية حقيقية. هذا هو
  النمط نفسه المرفوض والموثّق في `config.py:43` (`LIVE_START_TS`).
- عمود ميت تماماً (`social_replies`, `are_top_traders`, `is_scam`,
  `platform_dev_holding`) يدخل النموذج بلا معلومة — كلفة لا ضرر.

فالحراسة الآن **بالعين لا بالكود**: أيّ AUC يقفز بعد إضافة عائلة جديدة يُشتبَه
فيه أوّلاً كتسريب حِقبة، ويُتحقّق بمقارنة تغطية العمود على نصفَي الإطار الزمنيّ
قبل الاعتماد عليه.

## لاحقاً (غير معطِّل الآن)

- **قرار النافذة (t0+Δ)**: صفوف قرار عند +10د/+30د بليبل من سعر دخولها هو
  (PLAN §2.3-ب) — المستخرج جاهز، ينتظر صفّ outcomes ثانياً بمفتاح `id:d30`.
- **مصنّف نصوص الأطروحات** بنموذج محليّ صغير (PLAN §2.3-و-ب): {جوهريّ · شيلّ ·
  محفّز · تردّد} — ميزة رجعية صالحة لأنّ النصّ ثابت منذ الكتابة.
