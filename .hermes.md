# تعليمات مشروع aoi — تُحمَّل في كل جلسة داخل هذا المجلد

## ما هو المشروع
نظام جمع بيانات منصّة fomo.family التداولية لحظةً بلحظة (لا API رسمي — تجاوز Cloudflare عبر curl_cffi)،
ويوسِم نتيجة كل إشارة بعد 48 ساعة، ويبني صفوف تدريب لنموذج يتنبّأ بأي عملة سترتفع بعد إشارة شراء.

## القواعد الصارمة (لا تُخالَف)
1. **قاعدة البيانات `recorder/recorder.db` قراءة فقط للوكيل** (`mode=ro`). لا INSERT/UPDATE/DDL مباشر إطلاقًا — الكتابة حصرًا عبر الخدمات السبع.
2. **لا تقتل أو تعيد جدولة مهام Windows المجدولة** (سبع خدمات تعمل بـ pythonw.exe بلا نوافذ وتنجو من إعادة التشغيل). إن بدت متوقفة، شخّص السبب أولًا عبر السجلات.
3. **الأسرار خارج الحدود**: `chain_keys.json`، `api/.env`، `api/.privy_state.json*` — لا تُقرأ محتوياتها ولا تُنسخ إلى مخرجات أو تقارير.
4. **بوابة المرحلة-1 ملزمة**: الضابطة v3 الناضجة عند 305 والعتبة الملزمة 500 لم تُبلَغ. **لا تدريب نموذج دخول معتمد** قبل بلوغها. العتبة الاستكشافية (100) مفتوحة.
5. كل تغيير ينتهي باختبارات خضراء (`./run_tests.ps1` أو `py -3 -m pytest recorder/tests/...`) قبل أي commit.

## المعمارية (سبع خدمات دائمة)
| الخدمة | الإيقاع | الدور |
|---|---|---|
| FomoApiServer :8080 | دائم | بوابة fomo.family + مصادقة Privy ذاتية التجديد |
| FomoRecorder | كل 60 ث | التقاط لحظة القرار t=0 |
| FomoChain | كل 60 ث | عقود Solana (Helius/RPC رسمية) |
| FomoEVMReplay | كل 60 ث | إعادة سلوك EVM |
| FomoLabeler | كل 15 د | توسيم النتائج بعد 48 ساعة |
| FomoBuildRows | كل ساعة | بناء صفوف التدريب |
| FomoDashboard :8090 | دائم | عرض قراءة فقط |
| FomoBackup | يوميًا 03:15 | نسخ احتياطي إلى OneDrive |

## خرائط مهمة
- **الخطة التفصيلية:** `docs/PLAN.md` · **تقارير الجاهزية:** `docs/data-readiness-*.md` (مولدة آليًا — لا أرقام يدوية)
- **مهام التنفيذ:** `tasks/todo.md` (نشطة) و`tasks/completed/` — المهمة تنتهي بتقرير تحقق قبل الانتقال للتالية
- **مخطط القاعدة:** 34 جدولًا — انظر مهارة `aoi-fomo-pipeline` للتصنيف الكامل
- **لغة التوثيق:** عربية فصحى بنفس أسلوب README.md

## حالة العمل الحالية (حدّثها مع التقدم)
- الجمع والتوسيم وبناء الصفوف تلقائية بالكامل؛ مستخرج الميزات يعمل
- آخر تدقيق بيانات: `docs/data-audit-2026-08-22.md`
- **بوابة العمر منفذة ومكتملة** (رفض العملات الأصغر من يومين عند لحظة الإشارة، `AGE_GATE_ENABLED_AT = 2026-08-22T18:03Z`): مطابقة عناوين EVM غير حساسة لحالة الأحراف وسولانا حساسة، parser زمني موحّد عبر `epoch_of`، quarantine يقارن epochs لا نصوص ISO. المجموعة الكاملة خضراء: **938 اختبارًا نجحت** (2026-08-24).
- **إصلاح بوابة إدخال EVM (2026-08-29، مكتمل ومختبر — 990 اختبارًا خضراء):** أربعة أعطاب شُخّصت على القاعدة الحيّة وطابور روبن‑هود المتوقف (43 عملة): (1) وحدات عمل بوابة الإدخال كانت تُقدَّر بسقف المدى 10K بينما النداء يقطع ~500K فعليًا (`EVM_BACKFILL_BLOCKS_PER_CALL` الجديد) — البوابة كانت مغلقة أبدًا؛ (2) سقف عمر أعلى لإدخال EVM `EVM_MAX_TOKEN_AGE_DAYS=60` (عملات سنة–سنتين كانت تدخل بملايين الكتل)؛ (3) فحص السياسة صار يُطبَّق على إعادة التنشيط لا الجديد فقط؛ (4) إعادة التنشيط بفجوة ≤48س تحفظ دفتر EVM ونقطة الاستئناف (`EVM_REACTIVATION_KEEP_LEDGER_SECONDS`) بدل الحذف الكامل. اختبارات: `tests/test_evm_admission_gate_fix.py` (11).
- ⚠️ كل تغييرات بوابة العمر **غير مودعة** على فرع `recorder-age-gate` (~30 ملفًا معدلًا) + ~300 سكربت `probe_*.py` غير متعقّب تحتاج فرزًا (إبقاء المهم منها كتقارير تحقق / حذف السريعة) قبل أي commit نظيف
- worktrees: واحد فقط الآن (`wakeful-closet` أُزيل بعد دمجه في جولات سابقة)
